# --- Cœur (back) — versions épinglées, à auditer périodiquement (pip-audit) ---
requests==2.34.2
PyJWT==2.14.0
cryptography==50.0.1

# --- Front (Streamlit) — testé avec ces versions ---
streamlit==1.58.0
plotly==6.8.0
pandas==3.0.2
